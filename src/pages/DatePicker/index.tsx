import React from "react";
import { BasicRangePicker } from "@components/DatePicker";

const BasicDatePicker = (props: any) => {
  return <BasicRangePicker {...props} />;
};

export default BasicDatePicker;
